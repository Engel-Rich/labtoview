import 'package:flutter/material.dart';

import '../../variables/themes.dart';

class DrawerBuil extends StatelessWidget {
  const DrawerBuil({super.key});

  @override
  Widget build(BuildContext context) {
    return const Drawer(backgroundColor: indigo);
  }
}
